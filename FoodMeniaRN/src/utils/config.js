const IP = "192.168.1.7";

export const CONFIG = {
  API_BASE: `http://${IP}:9090/api`,
  UPLOADS_BASE: `http://${IP}:9090`,
};

export const ROLES = {
  user: "user",
  operator: "operator",
  creator: "creator",
};